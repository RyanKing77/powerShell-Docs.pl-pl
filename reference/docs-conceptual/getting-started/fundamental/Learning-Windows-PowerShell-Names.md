---
ms.date: 06/05/2017
keywords: polecenia cmdlet programu PowerShell
title: Poznawanie nazw programu Windows PowerShell
ms.assetid: b4d0fd22-8298-4ee6-82ae-9b6f2907c986
ms.openlocfilehash: 381aa619a41ccacb2ff3a4cdbc2b75b7f04282d1
ms.sourcegitcommit: cf195b090b3223fa4917206dfec7f0b603873cdf
ms.translationtype: MT
ms.contentlocale: pl-PL
ms.lasthandoff: 04/09/2018
---
# <a name="learning-windows-powershell-names"></a><span data-ttu-id="e3a6c-103">Poznawanie nazw programu Windows PowerShell</span><span class="sxs-lookup"><span data-stu-id="e3a6c-103">Learning Windows PowerShell Names</span></span>
<span data-ttu-id="e3a6c-104">Learning nazwy poleceń i parametrów polecenia jest inwestycji długiego czasu, większość interfejsów wiersza polecenia.</span><span class="sxs-lookup"><span data-stu-id="e3a6c-104">Learning names of commands and command parameters is a significant time investment with most command-line interfaces.</span></span> <span data-ttu-id="e3a6c-105">Problem polega na tym, czy bardzo mało wzorców, więc jedynym sposobem, aby dowiedzieć się, jest tylko każdego polecenia i każdego parametru, który musi być na bieżąco.</span><span class="sxs-lookup"><span data-stu-id="e3a6c-105">The issue is that there are very few patterns, so the only way to learn is by memorizing each command and each parameter that you need to use on a regular basis.</span></span>

<span data-ttu-id="e3a6c-106">Podczas pracy z nowego polecenia lub parametr, zwykle można co znasz już; należy znaleźć i Dowiedz się nową nazwę.</span><span class="sxs-lookup"><span data-stu-id="e3a6c-106">When you work with a new command or parameter, you cannot generally use what you already know; you have to find and learn a new name.</span></span> <span data-ttu-id="e3a6c-107">Jeśli przyjrzymy się jak Zwiększ interfejsy z niewielki zestaw narzędzi z dodatkami przyrostowe do funkcji, jest łatwo sprawdzić, dlaczego struktury jest niestandardowy.</span><span class="sxs-lookup"><span data-stu-id="e3a6c-107">If you look at how interfaces grow from a small set of tools with incremental additions to functionality, it is easy to see why the structure is nonstandard.</span></span> <span data-ttu-id="e3a6c-108">Z nazw poleceń w szczególności, to może dźwiękowej logicznej ponieważ każde polecenie jest narzędziem do oddzielnych, ale lepiej obsługiwać nazw poleceń.</span><span class="sxs-lookup"><span data-stu-id="e3a6c-108">With command names in particular, this may sound logical since each command is a separate tool, but there is a better way to handle command names.</span></span>

<span data-ttu-id="e3a6c-109">Większość używanych poleceń są przeznaczone do zarządzania elementami systemu operacyjnego lub aplikacji, takich jak usług lub procesów.</span><span class="sxs-lookup"><span data-stu-id="e3a6c-109">Most commands are built to manage elements of the operating system or applications, such as services or processes.</span></span> <span data-ttu-id="e3a6c-110">Polecenia mają różne nazwy, które mogą lub mogą nie pasować do rodziny.</span><span class="sxs-lookup"><span data-stu-id="e3a6c-110">The commands have a variety of names that may or may not fit into a family.</span></span> <span data-ttu-id="e3a6c-111">Na przykład w systemach Windows, można użyć **net start** i **net stop** polecenia, aby uruchomić i zatrzymać usługi.</span><span class="sxs-lookup"><span data-stu-id="e3a6c-111">For example, on Windows systems, you can use the **net start** and **net stop** commands to start and stop a service.</span></span> <span data-ttu-id="e3a6c-112">Istnieje inne narzędzie kontroli usługi bardziej ogólną dla systemu Windows o innej nazwie, **sc**, który nie pasuje do wzorca nazewnictwa dla **net** obsługi poleceń.</span><span class="sxs-lookup"><span data-stu-id="e3a6c-112">There is another more generalized service control tool for Windows that has a completely different name, **sc**, that does not fit into the naming pattern for the **net** service commands.</span></span> <span data-ttu-id="e3a6c-113">Zarządzanie procesem, system Windows ma **tasklist** polecenia do listy procesów i **taskkill** polecenia kill procesów.</span><span class="sxs-lookup"><span data-stu-id="e3a6c-113">For process management, Windows has the **tasklist** command to list processes and the **taskkill** command to kill processes.</span></span>

<span data-ttu-id="e3a6c-114">Polecenia, które przyjmują parametry mają specyfikacje nieprawidłowych parametrów.</span><span class="sxs-lookup"><span data-stu-id="e3a6c-114">Commands that take parameters have irregular parameter specifications.</span></span> <span data-ttu-id="e3a6c-115">Nie można użyć **net start** polecenie, aby uruchomić usługę na komputerze zdalnym.</span><span class="sxs-lookup"><span data-stu-id="e3a6c-115">You cannot use the **net start** command to start a service on a remote computer.</span></span> <span data-ttu-id="e3a6c-116">**Sc** polecenia powoduje uruchomienie usługi na komputerze zdalnym, ale aby określić komputer zdalny, możesz jej nazwę musi poprzedzać prefiks podwójny ukośnik odwrotny.</span><span class="sxs-lookup"><span data-stu-id="e3a6c-116">The **sc** command will start a service on a remote computer, but to specify the remote computer, you must prefix its name with a double backslash.</span></span> <span data-ttu-id="e3a6c-117">Na przykład, aby uruchomić usługę buforu wydruku na komputerze zdalnym o nazwie DC01, należy wpisać **sc \\ \\DC01 start buforu**.</span><span class="sxs-lookup"><span data-stu-id="e3a6c-117">For example, to start the spooler service on a remote computer named DC01, you would type **sc \\\\DC01 start spooler**.</span></span> <span data-ttu-id="e3a6c-118">Do listy zadań uruchomionych na DC01, należy użyć **/S** parametr (dla "systemu") i podaj nazwę DC01 bez ukośników odwrotnych w następujący sposób: **tasklist /S DC01**.</span><span class="sxs-lookup"><span data-stu-id="e3a6c-118">To list tasks running on DC01, you need to use the **/S** (for "system") parameter and supply the name DC01 without backslashes, like this: **tasklist /S DC01**.</span></span>

<span data-ttu-id="e3a6c-119">Chociaż są ważne techniczne różnice między usługą i procesem, są oba przykłady elementów można zarządzać na komputerze, które mają dobrze zdefiniowanego cyklu życia.</span><span class="sxs-lookup"><span data-stu-id="e3a6c-119">Although there are important technical distinctions between a service and a process, they are both examples of manageable elements on a computer that have a well-defined life cycle.</span></span> <span data-ttu-id="e3a6c-120">Możesz rozpocząć lub zatrzymać usługi lub procesu lub uzyskać listę wszystkich aktualnie uruchomione usług lub procesów.</span><span class="sxs-lookup"><span data-stu-id="e3a6c-120">You may want to start or stop a service or process, or get a list of all currently running services or processes.</span></span> <span data-ttu-id="e3a6c-121">Innymi słowy mimo że usługę i procesem są różne, akcje, które na usługi lub procesu są często koncepcyjnie takie same.</span><span class="sxs-lookup"><span data-stu-id="e3a6c-121">In other words, although a service and a process are different things, the actions we perform on a service or a process are often conceptually the same.</span></span> <span data-ttu-id="e3a6c-122">Ponadto opcje, które firma Microsoft może wprowadzać można dostosować, określając parametry akcji mogą być podobny również.</span><span class="sxs-lookup"><span data-stu-id="e3a6c-122">Furthermore, choices we may make to customize an action by specifying parameters may be conceptually similar as well.</span></span>

<span data-ttu-id="e3a6c-123">Środowisko Windows PowerShell wykorzystuje te podobieństwa, aby zmniejszyć liczbę unikatowych nazw musisz wiedzieć, aby zrozumieć i użyć polecenia cmdlet.</span><span class="sxs-lookup"><span data-stu-id="e3a6c-123">Windows PowerShell exploits these similarities to reduce the number of distinct names you need to know to understand and use cmdlets.</span></span>

### <a name="cmdlets-use-verb-noun-names-to-reduce-command-memorization"></a><span data-ttu-id="e3a6c-124">Użyj polecenia cmdlet czasownik rzeczownik nazw zmniejszenie Memorization polecenia</span><span class="sxs-lookup"><span data-stu-id="e3a6c-124">Cmdlets Use Verb-Noun Names to Reduce Command Memorization</span></span>
<span data-ttu-id="e3a6c-125">Program Windows PowerShell korzysta "czasownik rzeczownik" systemem nazw, gdy każda nazwa polecenia cmdlet składa się z standardowe zlecenie dzielone z określonych rzeczownik.</span><span class="sxs-lookup"><span data-stu-id="e3a6c-125">Windows PowerShell uses a "verb-noun" naming system, where each cmdlet name consists of a standard verb hyphenated with a specific noun.</span></span> <span data-ttu-id="e3a6c-126">Poleceń programu Windows PowerShell nie zawsze są zleceń w języku angielskim, ale one express określone czynności w programie Windows PowerShell.</span><span class="sxs-lookup"><span data-stu-id="e3a6c-126">Windows PowerShell verbs are not always English verbs, but they express specific actions in Windows PowerShell.</span></span> <span data-ttu-id="e3a6c-127">Rzeczowniki są bardzo podobne rzeczowniki w dowolnym języku, opisano w nich określonych typów obiektów, które są ważne w witrynie Administracja systemu.</span><span class="sxs-lookup"><span data-stu-id="e3a6c-127">Nouns are very much like nouns in any language, they describe specific types of objects that are important in system administration.</span></span> <span data-ttu-id="e3a6c-128">To proste zademonstrować, jak te nazwy dwuczęściową ograniczyć learning analizując kilka przykładów zleceń i rzeczowniki.</span><span class="sxs-lookup"><span data-stu-id="e3a6c-128">It is easy to demonstrate how these two-part names reduce learning effort by looking at a few examples of verbs and nouns.</span></span>

<span data-ttu-id="e3a6c-129">Rzeczowniki są mniej ograniczone, ale należy zawsze opisano polecenie działa na.</span><span class="sxs-lookup"><span data-stu-id="e3a6c-129">Nouns are less restricted, but they should always describe what a command acts upon.</span></span> <span data-ttu-id="e3a6c-130">Środowisko Windows PowerShell jest polecenia **Get-Process**, **Stop-Process**, **Get-Service**, i **Stop-Service**.</span><span class="sxs-lookup"><span data-stu-id="e3a6c-130">Windows PowerShell has commands such as **Get-Process**, **Stop-Process**, **Get-Service**, and **Stop-Service**.</span></span>

<span data-ttu-id="e3a6c-131">W przypadku dwóch rzeczowniki i dwóch zleceń spójności nie uprościć, learning fragment odpowiadający.</span><span class="sxs-lookup"><span data-stu-id="e3a6c-131">In the case of two nouns and two verbs, consistency does not simplify learning that much.</span></span> <span data-ttu-id="e3a6c-132">Jednak jeśli przyjrzymy się standardowy zestaw 10 zleceń i 10 rzeczowniki, można wybrać tylko 20 słów, aby zrozumieć, ale te słowa może służyć do tworzenia 100 polecenia unikatowych nazw.</span><span class="sxs-lookup"><span data-stu-id="e3a6c-132">However, if you look at a standard set of 10 verbs and 10 nouns, you then have only 20 words to understand, but those words can be used to form 100 distinct command names.</span></span>

<span data-ttu-id="e3a6c-133">Często może rozpoznać, odczytując nazwy działania polecenia i zwykle okaże się, jaką nazwę ma być używane jako nowe polecenie.</span><span class="sxs-lookup"><span data-stu-id="e3a6c-133">Frequently, you can recognize what a command does by reading its name, and it is usually apparent what name should be used for a new command.</span></span> <span data-ttu-id="e3a6c-134">Na przykład polecenie zamknięcia komputera może być **Stop-Computer**.</span><span class="sxs-lookup"><span data-stu-id="e3a6c-134">For example, a computer shutdown command might be **Stop-Computer**.</span></span> <span data-ttu-id="e3a6c-135">Polecenie, które wyświetla listę wszystkich komputerów w sieci może być **Get-Computer**.</span><span class="sxs-lookup"><span data-stu-id="e3a6c-135">A command that lists all computers on a network might be **Get-Computer**.</span></span> <span data-ttu-id="e3a6c-136">To polecenie pobiera datę systemową **Get-Date**.</span><span class="sxs-lookup"><span data-stu-id="e3a6c-136">The command that gets the system date is **Get-Date**.</span></span>

<span data-ttu-id="e3a6c-137">Możesz wyświetlić listę wszystkich poleceń, które zawierają konkretnego zlecenia z **— zlecenie** parametr **Get-Command** (omówimy **Get-Command** szczegółowo w następnej sekcji).</span><span class="sxs-lookup"><span data-stu-id="e3a6c-137">You can list all commands that include a particular verb with the **-Verb** parameter for **Get-Command** (We will discuss **Get-Command** in detail in the next section).</span></span> <span data-ttu-id="e3a6c-138">Na przykład, aby wyświetlić wszystkie polecenia cmdlet, które używają zlecenie **uzyskać**, wpisz:</span><span class="sxs-lookup"><span data-stu-id="e3a6c-138">For example, to see all cmdlets that use the verb **Get**, type:</span></span>

```
PS> Get-Command -Verb Get
CommandType     Name                            Definition
-----------     ----                            ----------
Cmdlet          Get-Acl                         Get-Acl [[-Path] <String[]>]...
Cmdlet          Get-Alias                       Get-Alias [[-Name] <String[]...
Cmdlet          Get-AuthenticodeSignature       Get-AuthenticodeSignature [-...
Cmdlet          Get-ChildItem                   Get-ChildItem [[-Path] <Stri...
...
```

<span data-ttu-id="e3a6c-139">**-Rzeczownik** parametr jest bardziej przydatne, ponieważ umożliwia Zobacz rodziny poleceń, które mają wpływ na ten sam typ obiektu.</span><span class="sxs-lookup"><span data-stu-id="e3a6c-139">The **-Noun** parameter is even more useful because it allows you to see a family of commands that affect the same type of object.</span></span> <span data-ttu-id="e3a6c-140">Na przykład jeśli chcesz zobaczyć, które polecenia są dostępne do zarządzania usługami, wpisz następujące polecenie:</span><span class="sxs-lookup"><span data-stu-id="e3a6c-140">For example, if you want to see which commands are available for managing services, type following command:</span></span>

```
PS> Get-Command -Noun Service
CommandType     Name                            Definition
-----------     ----                            ----------
Cmdlet          Get-Service                     Get-Service [[-Name] <String...
Cmdlet          New-Service                     New-Service [-Name] <String>...
Cmdlet          Restart-Service                 Restart-Service [-Name] <Str...
Cmdlet          Resume-Service                  Resume-Service [-Name] <Stri...
Cmdlet          Set-Service                     Set-Service [-Name] <String>...
Cmdlet          Start-Service                   Start-Service [-Name] <Strin...
Cmdlet          Stop-Service                    Stop-Service [-Name] <String...
Cmdlet          Suspend-Service                 Suspend-Service [-Name] <Str...
...
```

<span data-ttu-id="e3a6c-141">Polecenie nie jest zawsze polecenia cmdlet, po prostu, ponieważ ma ona schemat nazewnictwa czasownik rzeczownik.</span><span class="sxs-lookup"><span data-stu-id="e3a6c-141">A command is not necessarily a cmdlet, just because it has a verb-noun naming scheme.</span></span> <span data-ttu-id="e3a6c-142">Przykładem natywnego polecenia programu Windows PowerShell, który nie jest poleceniem cmdlet, ale nazwa czasownik rzeczownik, to polecenie czyszczenia okna konsoli, wyczyść hosta.</span><span class="sxs-lookup"><span data-stu-id="e3a6c-142">One example of a native Windows PowerShell command that is not a cmdlet but has a verb-noun name, is the command for clearing a console window, Clear-Host.</span></span> <span data-ttu-id="e3a6c-143">Polecenie Wyczyść hosta jest rzeczywiście funkcji wewnętrznej, jak widać, jeśli Get-Command Uruchom na niej:</span><span class="sxs-lookup"><span data-stu-id="e3a6c-143">The Clear-Host command is actually an internal function, as you can see if you run Get-Command against it:</span></span>

```
PS> Get-Command -Name Clear-Host

CommandType     Name                            Definition
-----------     ----                            ----------
Function        Clear-Host                      $spaceType = [System.Managem...
```

### <a name="cmdlets-use-standard-parameters"></a><span data-ttu-id="e3a6c-144">Użyj polecenia cmdlet standardowe parametry</span><span class="sxs-lookup"><span data-stu-id="e3a6c-144">Cmdlets Use Standard Parameters</span></span>
<span data-ttu-id="e3a6c-145">Jak wspomniano wcześniej, polecenia używane w tradycyjnych interfejsów wiersza polecenia nie ma zazwyczaj nazwy parametrów zgodne.</span><span class="sxs-lookup"><span data-stu-id="e3a6c-145">As noted earlier, commands used in traditional command-line interfaces do not generally have consistent parameter names.</span></span> <span data-ttu-id="e3a6c-146">Czasami parametry bez nazwy w ogóle.</span><span class="sxs-lookup"><span data-stu-id="e3a6c-146">Sometimes parameters do not have names at all.</span></span> <span data-ttu-id="e3a6c-147">Gdy tak robią, są często jednym znaku lub skrócone słowa, które można wpisać szybko, ale nie są zrozumiałe dla nowych użytkowników.</span><span class="sxs-lookup"><span data-stu-id="e3a6c-147">When they do, they are often single-character or abbreviated words that can be typed rapidly but are not easily understood by new users.</span></span>

<span data-ttu-id="e3a6c-148">W przeciwieństwie do większości innych tradycyjnych interfejsów wiersza polecenia programu Windows PowerShell bezpośrednio przetwarza parametry i używa tego bezpośredni dostęp do parametrów oraz wskazówki dla deweloperów normalizacji nazwy parametrów.</span><span class="sxs-lookup"><span data-stu-id="e3a6c-148">Unlike most other traditional command-line interfaces, Windows PowerShell processes parameters directly, and it uses this direct access to the parameters along with developer guidance to standardize parameter names.</span></span> <span data-ttu-id="e3a6c-149">Mimo że nie gwarantuje to, że każdy polecenia cmdlet będzie zawsze ze standardami, jego zachęca.</span><span class="sxs-lookup"><span data-stu-id="e3a6c-149">Although this does not guarantee that every cmdlet will always conform to the standards, it does encourage it.</span></span>

> [!NOTE]
> <span data-ttu-id="e3a6c-150">Zawsze mają nazwy parametru "-" dołączany na początku ich użycia, aby umożliwić programowi Windows PowerShell jednoznacznie zidentyfikować je jako parametry.</span><span class="sxs-lookup"><span data-stu-id="e3a6c-150">Parameter names always have a '-' prepended to them when you use them, to allow Windows PowerShell to clearly identify them as parameters.</span></span> <span data-ttu-id="e3a6c-151">W **Get-Command - Name Clear-Host** przykładzie nazwa parametru jest **nazwa**, ale jest wprowadzana jako -**nazwa**.</span><span class="sxs-lookup"><span data-stu-id="e3a6c-151">In the **Get-Command -Name Clear-Host** example, the parameter's name is **Name**, but it is entered as -**Name**.</span></span>

<span data-ttu-id="e3a6c-152">Poniżej przedstawiono ogólne właściwości nazwy parametrów standardowe i ich użycia.</span><span class="sxs-lookup"><span data-stu-id="e3a6c-152">Here are some of the general characteristics of the standard parameter names and usages.</span></span>

#### <a name="the-help-parameter-"></a><span data-ttu-id="e3a6c-153">Parametr pomocy (?)</span><span class="sxs-lookup"><span data-stu-id="e3a6c-153">The Help Parameter (?)</span></span>
<span data-ttu-id="e3a6c-154">Po określeniu **-?**</span><span class="sxs-lookup"><span data-stu-id="e3a6c-154">When you specify the **-?**</span></span> <span data-ttu-id="e3a6c-155">parametr do każdego polecenia cmdlet, polecenie cmdlet nie została wykonana.</span><span class="sxs-lookup"><span data-stu-id="e3a6c-155">parameter to any cmdlet, the cmdlet is not executed.</span></span> <span data-ttu-id="e3a6c-156">Zamiast tego środowiska Windows PowerShell Wyświetla Pomoc dotyczącą polecenia cmdlet.</span><span class="sxs-lookup"><span data-stu-id="e3a6c-156">Instead, Windows PowerShell displays help for the cmdlet.</span></span>

#### <a name="common-parameters"></a><span data-ttu-id="e3a6c-157">Wspólne parametry</span><span class="sxs-lookup"><span data-stu-id="e3a6c-157">Common Parameters</span></span>
<span data-ttu-id="e3a6c-158">Środowisko Windows PowerShell zawiera kilka parametrów, znany jako *wspólne parametry*.</span><span class="sxs-lookup"><span data-stu-id="e3a6c-158">Windows PowerShell has several parameters known as *common parameters*.</span></span> <span data-ttu-id="e3a6c-159">Ponieważ te parametry są kontrolowane przez aparat środowiska Windows PowerShell w każdym przypadku, gdy są implementowane za pomocą polecenia cmdlet, będzie zawsze zachowują się tak samo.</span><span class="sxs-lookup"><span data-stu-id="e3a6c-159">Because these parameters are controlled by the Windows PowerShell engine, whenever they are implemented by a cmdlet, they will always behave the same way.</span></span> <span data-ttu-id="e3a6c-160">Wspólne parametry są **WhatIf**, **Potwierdź**, **pełne**, **debugowania**, **Ostrzegaj**, **ErrorAction**, **ErrorVariable**, **OutVariable**, i **OutBuffer**.</span><span class="sxs-lookup"><span data-stu-id="e3a6c-160">The common parameters are **WhatIf**, **Confirm**, **Verbose**, **Debug**, **Warn**, **ErrorAction**, **ErrorVariable**, **OutVariable**, and **OutBuffer**.</span></span>

#### <a name="suggested-parameters"></a><span data-ttu-id="e3a6c-161">Sugerowane parametrów</span><span class="sxs-lookup"><span data-stu-id="e3a6c-161">Suggested Parameters</span></span>
<span data-ttu-id="e3a6c-162">Podstawowych poleceń cmdlet programu Windows PowerShell Użyj standardowych nazw dla podobnych parametrów.</span><span class="sxs-lookup"><span data-stu-id="e3a6c-162">The Windows PowerShell core cmdlets use standard names for similar parameters.</span></span> <span data-ttu-id="e3a6c-163">Mimo że użycia nazwy parametrów nie jest wymuszana, nie istnieje jawne wskazówki dotyczące użycia wspieranie normalizacji.</span><span class="sxs-lookup"><span data-stu-id="e3a6c-163">Although the use of parameter names is not enforced, there is explicit guidance for usage to encourage standardization.</span></span>

<span data-ttu-id="e3a6c-164">Na przykład wskazówki zaleca parametr nazewnictwa, która odnosi się do komputera o nazwie, co **ComputerName**, a nie serwera hosta, System, węzła albo innych popularnych wyrazów alternatywnych.</span><span class="sxs-lookup"><span data-stu-id="e3a6c-164">For example, the guidance recommends naming a parameter that refers to a computer by name as **ComputerName**, rather than Server, Host, System, Node, or other common alternative words.</span></span> <span data-ttu-id="e3a6c-165">Wśród ważne parametru sugerowane nazwy są **życie**, **wykluczyć**, **Include**, **PassThru**, **ścieżka**, i **CaseSensitive**.</span><span class="sxs-lookup"><span data-stu-id="e3a6c-165">Among the important suggested parameter names are **Force**, **Exclude**, **Include**, **PassThru**, **Path**, and **CaseSensitive**.</span></span>