---
ms.date: 05/17/2018
keywords: środowiska PowerShell, podstawowe
title: Znane problemy dotyczące programu PowerShell w wersji 6.0
ms.openlocfilehash: 6ad1bcaf1de06f204b57eb8ce23b3053ba4a5b38
ms.sourcegitcommit: 2d9cf1ccb9a653db7726a408ebcb65530dcb1522
ms.translationtype: MT
ms.contentlocale: pl-PL
ms.lasthandoff: 05/19/2018
---
# <a name="known-issues-for-powershell-60"></a><span data-ttu-id="d8aad-103">Znane problemy dotyczące programu PowerShell w wersji 6.0</span><span class="sxs-lookup"><span data-stu-id="d8aad-103">Known Issues for PowerShell 6.0</span></span>

## <a name="known-issues-for-powershell-on-non-windows-platforms"></a><span data-ttu-id="d8aad-104">Znane problemy dotyczące programu PowerShell na platformach systemu Windows</span><span class="sxs-lookup"><span data-stu-id="d8aad-104">Known Issues for PowerShell on Non-Windows Platforms</span></span>

<span data-ttu-id="d8aad-105">Alfa wersje programu PowerShell w systemie Linux i macOS przede wszystkim funkcjonalności, którzy mają pewnych ograniczeń znaczących i problemów z użytecznością.</span><span class="sxs-lookup"><span data-stu-id="d8aad-105">Alpha releases of PowerShell on Linux and macOS are mostly functional but do have some significant limitations and usability issues.</span></span> <span data-ttu-id="d8aad-106">Wydanie beta programu PowerShell w systemie Linux i macOS więcej funkcjonalności i stabilnego niż alfa wersje, ale nadal może brakuje niektórych zestaw funkcji i mogą zawierać błędy.</span><span class="sxs-lookup"><span data-stu-id="d8aad-106">Beta releases of PowerShell on Linux and macOS are more functional and stable than alpha releases, but still may be lacking some set of features, and can contain bugs.</span></span> <span data-ttu-id="d8aad-107">W niektórych przypadkach te problemy są po prostu usterki, które nie zostały jeszcze rozwiązane.</span><span class="sxs-lookup"><span data-stu-id="d8aad-107">In some cases, these issues are simply bugs that haven't been fixed yet.</span></span> <span data-ttu-id="d8aad-108">W innych przypadkach (tak jak domyślne aliasy ls cp, itp.), czekamy na opinie od społeczności dotyczące opcji wykonujemy.</span><span class="sxs-lookup"><span data-stu-id="d8aad-108">In other cases (as with the default aliases for ls, cp, etc.), we are looking for feedback from the community regarding the choices we make.</span></span>

<span data-ttu-id="d8aad-109">Uwaga: Z powodu podobieństwa wiele podstawowych podsystemów, programu PowerShell w systemie Linux i macOS zwykle na tym samym poziomie dojrzałości w funkcji i błędów.</span><span class="sxs-lookup"><span data-stu-id="d8aad-109">Note: Due to the similarities of many underlying subsystems, PowerShell on Linux and macOS tend to share the same level of maturity in both features and bugs.</span></span> <span data-ttu-id="d8aad-110">Z wyjątkiem przypadków wymienionych poniżej, problemy w tej sekcji dotyczą obu systemów operacyjnych.</span><span class="sxs-lookup"><span data-stu-id="d8aad-110">Except as noted below, the issues in this section will apply to both operating systems.</span></span>

### <a name="case-sensitivity-in-powershell"></a><span data-ttu-id="d8aad-111">Opcja uwzględniania wielkości liter w programie PowerShell</span><span class="sxs-lookup"><span data-stu-id="d8aad-111">Case-sensitivity in PowerShell</span></span>

<span data-ttu-id="d8aad-112">W przeszłości PowerShell została jednolicie bez uwzględniania wielkości liter, z pewnymi wyjątkami.</span><span class="sxs-lookup"><span data-stu-id="d8aad-112">Historically, PowerShell has been uniformly case-insensitive, with few exceptions.</span></span> <span data-ttu-id="d8aad-113">W systemach operacyjnych UNIX przypominającej systemu plików jest głównie z uwzględnieniem wielkości liter i programu PowerShell jest zgodna ze standardem systemu plików; jest to za pośrednictwem na wiele sposobów, oczywiste i oczywiste.</span><span class="sxs-lookup"><span data-stu-id="d8aad-113">On UNIX-like operating systems, the file system is predominantly case-sensitive and PowerShell adheres to the standard of the file system; this is exposed through a number of ways, obvious and non-obvious.</span></span>

#### <a name="directly"></a><span data-ttu-id="d8aad-114">bezpośrednio</span><span class="sxs-lookup"><span data-stu-id="d8aad-114">Directly</span></span>

- <span data-ttu-id="d8aad-115">Podczas określania pliku w programie PowerShell, należy użyć poprawną wielkość.</span><span class="sxs-lookup"><span data-stu-id="d8aad-115">When specifying a file in PowerShell, the correct case must be used.</span></span>

#### <a name="indirectly"></a><span data-ttu-id="d8aad-116">Pośrednio</span><span class="sxs-lookup"><span data-stu-id="d8aad-116">Indirectly</span></span>

- <span data-ttu-id="d8aad-117">Jeśli skrypt próbuje załadować modułu, a nazwa modułu jest nie z uwzględnieniem wielkości liter, załadowanie modułu zakończy się niepowodzeniem.</span><span class="sxs-lookup"><span data-stu-id="d8aad-117">If a script tries to load a module and the module name is not cased correctly, then the module load will fail.</span></span> <span data-ttu-id="d8aad-118">Może to spowodować problem z istniejących skryptów, jeśli nazwa za pomocą którego istnieje odwołanie do modułu nie pasuje do nazwy pliku.</span><span class="sxs-lookup"><span data-stu-id="d8aad-118">This may cause a problem with existing scripts if the name by which the module is referenced doesn't match the actual file name.</span></span>
- <span data-ttu-id="d8aad-119">Uzupełnianie po naciśnięciu tabulatora zostanie nie Ukończ automatycznie, jeśli przypadek nazwa pliku jest nieprawidłowy.</span><span class="sxs-lookup"><span data-stu-id="d8aad-119">Tab-completion will not auto-complete if the file name case is wrong.</span></span> <span data-ttu-id="d8aad-120">Fragment do wykonania musi być z uwzględnieniem wielkości liter poprawnie.</span><span class="sxs-lookup"><span data-stu-id="d8aad-120">The fragment to complete must be cased properly.</span></span> <span data-ttu-id="d8aad-121">(Bez uwzględniania wielkości liter dla wpisz nazwę i typ elementu członkowskiego zakończeń jest ukończenia).</span><span class="sxs-lookup"><span data-stu-id="d8aad-121">(Completion is case-insensitive for type name and type member completions.)</span></span>

### <a name="ps1-file-extensions"></a><span data-ttu-id="d8aad-122">. PS1 Rozszerzenia plików</span><span class="sxs-lookup"><span data-stu-id="d8aad-122">.PS1 File Extensions</span></span>

<span data-ttu-id="d8aad-123">Skrypty programu PowerShell musi kończyć się `.ps1` dla interpretera zrozumienie, jak załadować i uruchamiać je w bieżącym procesie.</span><span class="sxs-lookup"><span data-stu-id="d8aad-123">PowerShell scripts must end in `.ps1` for the interpreter to understand how to load and run them in the current process.</span></span> <span data-ttu-id="d8aad-124">Uruchamianie skryptów w bieżącym procesie jest zwykle oczekiwane zachowanie środowiska PowerShell.</span><span class="sxs-lookup"><span data-stu-id="d8aad-124">Running scripts in the current process is the expected usual behavior for PowerShell.</span></span> <span data-ttu-id="d8aad-125">`#!` Magiczna liczba mogą być dodawane do skryptu, który nie ma `.ps1` rozszerzenie, ale spowoduje, że skrypt do uruchomienia w nowym wystąpieniu programu PowerShell uniemożliwia skrypt pracy poprawnie podczas zamiany obiektów.</span><span class="sxs-lookup"><span data-stu-id="d8aad-125">The `#!` magic number may be added to a script that doesn't have a `.ps1` extension, but this will cause the script to be run in a new PowerShell instance preventing the script from working properly when interchanging objects.</span></span> <span data-ttu-id="d8aad-126">(Uwaga: może to być pożądane zachowanie podczas wykonywania skryptu programu PowerShell z `bash` lub innego powłoki.)</span><span class="sxs-lookup"><span data-stu-id="d8aad-126">(Note: this may be the desirable behavior when executing a PowerShell script from `bash` or another shell.)</span></span>

### <a name="missing-command-aliases"></a><span data-ttu-id="d8aad-127">Brak aliasy poleceń</span><span class="sxs-lookup"><span data-stu-id="d8aad-127">Missing command aliases</span></span>

<span data-ttu-id="d8aad-128">W systemie Linux/macOS, "aliasów wygody" dla podstawowych poleceń `ls`, `cp`, `mv`, `rm`, `cat`, `man`, `mount`, `ps` zostały usunięte.</span><span class="sxs-lookup"><span data-stu-id="d8aad-128">On Linux/macOS, the "convenience aliases" for the basic commands `ls`, `cp`, `mv`, `rm`, `cat`, `man`, `mount`, `ps` have been removed.</span></span> <span data-ttu-id="d8aad-129">W systemie Windows PowerShell udostępnia zestaw aliasy mapowane na nazwy polecenia systemu Linux dla wygody użytkownika.</span><span class="sxs-lookup"><span data-stu-id="d8aad-129">On Windows, PowerShell provides a set of aliases that map to Linux command names for user convenience.</span></span> <span data-ttu-id="d8aad-130">Następujące aliasy zostały usunięte z domyślnej programu PowerShell na dystrybucje systemu Linux/macOS, dzięki czemu natywnego pliku wykonywalnego do uruchomienia bez określania ścieżki.</span><span class="sxs-lookup"><span data-stu-id="d8aad-130">These aliases have been removed from the default PowerShell on Linux/macOS distributions, allowing the native executable to be run without specifying a path.</span></span>

<span data-ttu-id="d8aad-131">Brak zalet i wad do tej czynności.</span><span class="sxs-lookup"><span data-stu-id="d8aad-131">There are pros and cons to doing this.</span></span> <span data-ttu-id="d8aad-132">Usuwanie aliasów przedstawia w środowisku macierzystym polecenia użytkownikowi programu PowerShell, ale zmniejsza funkcjonalność w powłoce, ponieważ natywnych poleceń zwraca ciągi zamiast obiektów.</span><span class="sxs-lookup"><span data-stu-id="d8aad-132">Removing the aliases exposes the native command experience to the PowerShell user, but reduces functionality in the shell because the native commands return strings instead of objects.</span></span>

> [!NOTE]
> <span data-ttu-id="d8aad-133">Jest to obszar, w którym opinii szuka zespołowi środowiska PowerShell.</span><span class="sxs-lookup"><span data-stu-id="d8aad-133">This is an area where the PowerShell team is looking for feedback.</span></span>
> <span data-ttu-id="d8aad-134">Co to jest preferowany rozwiązania?</span><span class="sxs-lookup"><span data-stu-id="d8aad-134">What is the preferred solution?</span></span> <span data-ttu-id="d8aad-135">Firma Microsoft pozostawienie bez zmian lub Dodaj aliasy wygody ponownie?</span><span class="sxs-lookup"><span data-stu-id="d8aad-135">Should we leave it as is or add the convenience aliases back?</span></span> <span data-ttu-id="d8aad-136">Zobacz [wystawiać #929](https://github.com/PowerShell/PowerShell/issues/929).</span><span class="sxs-lookup"><span data-stu-id="d8aad-136">See [Issue #929](https://github.com/PowerShell/PowerShell/issues/929).</span></span>

### <a name="missing-wildcard-globbing-support"></a><span data-ttu-id="d8aad-137">Brak symbolu wieloznacznego (globbing) pomocy technicznej</span><span class="sxs-lookup"><span data-stu-id="d8aad-137">Missing Wildcard (globbing) Support</span></span>

<span data-ttu-id="d8aad-138">Obecnie PowerShell tylko ma rozwijanie symbolu wieloznacznego (globbing), wbudowanego polecenia cmdlet w systemie Windows oraz poleceń zewnętrznych lub pliki binarne, jak również polecenia cmdlet w systemie Linux.</span><span class="sxs-lookup"><span data-stu-id="d8aad-138">Currently, PowerShell only does wildcard expansion (globbing) for built-in cmdlets on Windows, and for external commands or binaries as well as cmdlets on Linux.</span></span> <span data-ttu-id="d8aad-139">Oznacza to, że polecenia, takich jak `ls
*.txt` zakończy się niepowodzeniem, ponieważ gwiazdki nie będą rozszerzane można dopasować nazw plików.</span><span class="sxs-lookup"><span data-stu-id="d8aad-139">This means that a command like `ls
*.txt` will fail because the asterisk will not be expanded to match file names.</span></span> <span data-ttu-id="d8aad-140">Można obejść to za pomocą ćwiczeń `ls (gci *.txt | % name)` lub po prostu, `gci *.txt` przy użyciu programu PowerShell wbudowanych odpowiednikiem `ls`.</span><span class="sxs-lookup"><span data-stu-id="d8aad-140">You can work around this by doing `ls (gci *.txt | % name)` or, more simply, `gci *.txt` using the PowerShell built-in equivalent to `ls`.</span></span>

<span data-ttu-id="d8aad-141">Zobacz [#954](https://github.com/PowerShell/PowerShell/issues/954) na przekazanie nam opinii na temat sposobów usprawnienia środowisko globbing w systemie Linux/macOS.</span><span class="sxs-lookup"><span data-stu-id="d8aad-141">See [#954](https://github.com/PowerShell/PowerShell/issues/954) to give us feedback on how to improve the globbing experience on Linux/macOS.</span></span>

### <a name="net-framework-vs-net-core-framework"></a><span data-ttu-id="d8aad-142">.NET framework w wersji programu vs .NET Core Framework</span><span class="sxs-lookup"><span data-stu-id="d8aad-142">.NET Framework vs .NET Core Framework</span></span>

<span data-ttu-id="d8aad-143">PowerShell w systemie Linux/macOS korzysta z platformy .NET Core, który jest podzbiorem pełnej .NET Framework Microsoft Windows.</span><span class="sxs-lookup"><span data-stu-id="d8aad-143">PowerShell on Linux/macOS uses .NET Core which is a subset of the full .NET Framework on Microsoft Windows.</span></span> <span data-ttu-id="d8aad-144">Jest to istotne, ponieważ PowerShell zapewnia bezpośredni dostęp do typów podstawowych framework, metody itp. W związku z tym skryptów uruchamianych w systemie Windows może nie działać na różnych platformach z systemem innym niż Windows z powodu różnic w struktury.</span><span class="sxs-lookup"><span data-stu-id="d8aad-144">This is significant because PowerShell provides direct access to the underlying framework types, methods, etc. As a result, scripts that run on Windows may not run on non-Windows platforms because of the differences in the frameworks.</span></span> <span data-ttu-id="d8aad-145">Aby uzyskać więcej informacji na temat platformy .NET Core Framework Zobacz <https://dotnetfoundation.org/net-core></span><span class="sxs-lookup"><span data-stu-id="d8aad-145">For more information about .NET Core Framework, see <https://dotnetfoundation.org/net-core></span></span>

<span data-ttu-id="d8aad-146">Pojawienie [.NET 2.0 standardowe](https://blogs.msdn.microsoft.com/dotnet/2016/09/26/introducing-net-standard/), .NET Core 2.0 będzie przywrócić wiele tradycyjnych typów i metod znajdujących się w pełnej .NET Framework.</span><span class="sxs-lookup"><span data-stu-id="d8aad-146">With the advent of [.NET Standard 2.0](https://blogs.msdn.microsoft.com/dotnet/2016/09/26/introducing-net-standard/), .NET Core 2.0 will bring back many of the traditional types and methods present in the full .NET Framework.</span></span> <span data-ttu-id="d8aad-147">Oznacza to, podstawowe programu PowerShell będzie można załadować wiele modułów środowiska Windows PowerShell tradycyjnych bez żadnych modyfikacji.</span><span class="sxs-lookup"><span data-stu-id="d8aad-147">This means that PowerShell Core will be able to load many traditional Windows PowerShell modules without modification.</span></span> <span data-ttu-id="d8aad-148">Możesz wykonać .NET Standard 2.0 pokrewne pracę [tutaj](https://github.com/PowerShell/PowerShell/projects/4).</span><span class="sxs-lookup"><span data-stu-id="d8aad-148">You can follow our .NET Standard 2.0 related work [here](https://github.com/PowerShell/PowerShell/projects/4).</span></span>

### <a name="redirection-issues"></a><span data-ttu-id="d8aad-149">Problemy z przekierowania</span><span class="sxs-lookup"><span data-stu-id="d8aad-149">Redirection Issues</span></span>

<span data-ttu-id="d8aad-150">Przekierowywanie wejściowy nie jest obsługiwane w programie PowerShell na dowolnej platformie.</span><span class="sxs-lookup"><span data-stu-id="d8aad-150">Input redirection is not supported in PowerShell on any platform.</span></span>
[<span data-ttu-id="d8aad-151">Problem #1629</span><span class="sxs-lookup"><span data-stu-id="d8aad-151">Issue #1629</span></span>](https://github.com/PowerShell/PowerShell/issues/1629)

<span data-ttu-id="d8aad-152">Użyj `Get-Content` do zapisywania zawartości pliku z potokiem.</span><span class="sxs-lookup"><span data-stu-id="d8aad-152">Use `Get-Content` to write the contents of a file into the pipeline.</span></span>

<span data-ttu-id="d8aad-153">Przekierowane dane wyjściowe będzie zawierać znacznika kolejności bajtów Unicode (BOM), gdy używane jest kodowanie UTF-8 domyślne.</span><span class="sxs-lookup"><span data-stu-id="d8aad-153">Redirected output will contain the Unicode byte order mark (BOM) when the default UTF-8 encoding is used.</span></span> <span data-ttu-id="d8aad-154">BOM powoduje problemów podczas pracy z narzędzia, które nie oczekują lub dołączanie do pliku.</span><span class="sxs-lookup"><span data-stu-id="d8aad-154">The BOM will cause problems when working with utilities that do not expect it or when appending to a file.</span></span> <span data-ttu-id="d8aad-155">Użyj `-Encoding Ascii` zapisać tekst w formacie ASCII (który nie jest Unicode, nie będą miały BOM).</span><span class="sxs-lookup"><span data-stu-id="d8aad-155">Use `-Encoding Ascii` to write ASCII text (which, not being Unicode, will not have a BOM).</span></span> <span data-ttu-id="d8aad-156">(Uwaga: zobacz [RFC0020](https://github.com/PowerShell/PowerShell-RFC/issues/71) Aby wysłać nam swoją opinię na temat zwiększania kodowania środowiska PowerShell podstawowych na wszystkich platformach.</span><span class="sxs-lookup"><span data-stu-id="d8aad-156">(Note: see [RFC0020](https://github.com/PowerShell/PowerShell-RFC/issues/71) to give us feedback on improving the encoding experience for PowerShell Core across all platforms.</span></span> <span data-ttu-id="d8aad-157">Jesteśmy pracy do obsługi UTF-8 bez BOM i może zmienić kodowania wartości domyślne dla poszczególnych poleceń cmdlet na platformach.)</span><span class="sxs-lookup"><span data-stu-id="d8aad-157">We are working to support UTF-8 without a BOM and potentially changing the encoding defaults for various cmdlets across platforms.)</span></span>

### <a name="job-control"></a><span data-ttu-id="d8aad-158">Zadanie kontroli</span><span class="sxs-lookup"><span data-stu-id="d8aad-158">Job Control</span></span>

<span data-ttu-id="d8aad-159">Brak obsługi sterowania zadania w programie PowerShell w systemie Linux/macOS.</span><span class="sxs-lookup"><span data-stu-id="d8aad-159">There is no job-control support in PowerShell on Linux/macOS.</span></span>
<span data-ttu-id="d8aad-160">`fg` i `bg` polecenia nie są dostępne.</span><span class="sxs-lookup"><span data-stu-id="d8aad-160">The `fg` and `bg` commands are not available.</span></span>

<span data-ttu-id="d8aad-161">W chwili obecnej, można użyć [zadania programu PowerShell](https://msdn.microsoft.com/powershell/reference/5.1/microsoft.powershell.core/about/about_jobs) które działają na wszystkich platformach.</span><span class="sxs-lookup"><span data-stu-id="d8aad-161">For the time being, you can use [PowerShell jobs](https://msdn.microsoft.com/powershell/reference/5.1/microsoft.powershell.core/about/about_jobs) which do work across all platforms.</span></span>

### <a name="remoting-support"></a><span data-ttu-id="d8aad-162">Obsługa komunikacji zdalnej</span><span class="sxs-lookup"><span data-stu-id="d8aad-162">Remoting Support</span></span>

<span data-ttu-id="d8aad-163">Obecnie PowerShell Core obsługuje komunikację zdalną środowiska PowerShell (PSRP) przez WSMan z uwierzytelnianiem podstawowym w macOS i Linux oraz z uwierzytelniania NTLM w systemie Linux.</span><span class="sxs-lookup"><span data-stu-id="d8aad-163">Currently, PowerShell Core supports PowerShell Remoting (PSRP) over WSMan with Basic authentication on macOS and Linux, and with NTLM-based authentication on Linux.</span></span> <span data-ttu-id="d8aad-164">(Uwierzytelnianie za pomocą protokołu Kerberos nie jest obsługiwana.)</span><span class="sxs-lookup"><span data-stu-id="d8aad-164">(Kerberos-based authentication is not supported.)</span></span>

<span data-ttu-id="d8aad-165">Pracy nad oparta na usłudze WSMan zdalnych jest wykonywana [psl omi dostawcy](https://github.com/PowerShell/psl-omi-provider) repozytorium.</span><span class="sxs-lookup"><span data-stu-id="d8aad-165">The work for WSMan-based remoting is being done in the [psl-omi-provider](https://github.com/PowerShell/psl-omi-provider) repo.</span></span>

<span data-ttu-id="d8aad-166">PowerShell Core obsługuje również komunikację zdalną środowiska PowerShell (PSRP) za pomocą protokołu SSH na wszystkich platformach (z systemem Windows, system macOS i Linux).</span><span class="sxs-lookup"><span data-stu-id="d8aad-166">PowerShell Core also supports PowerShell Remoting (PSRP) over SSH on all platforms (Windows, macOS, and Linux).</span></span> <span data-ttu-id="d8aad-167">Podczas to nie jest obecnie obsługiwane w środowisku produkcyjnym, możesz dowiedzieć się więcej o konfigurowania [tutaj](../core-powershell/ssh-remoting-in-powershell-core.md).</span><span class="sxs-lookup"><span data-stu-id="d8aad-167">While this is not currently supported in production, you can learn more about setting this up [here](../core-powershell/ssh-remoting-in-powershell-core.md).</span></span>

### <a name="just-enough-administration-jea-support"></a><span data-ttu-id="d8aad-168">Obsługa Just dostatecznie administracyjnej (JEA)</span><span class="sxs-lookup"><span data-stu-id="d8aad-168">Just-Enough-Administration (JEA) Support</span></span>

<span data-ttu-id="d8aad-169">Możliwość tworzenia ograniczonego administrowanie punktów końcowych usług zdalnych (JEA) nie jest obecnie dostępne w programie PowerShell w systemie Linux/macOS.</span><span class="sxs-lookup"><span data-stu-id="d8aad-169">The ability to create constrained administration (JEA) remoting endpoints is not currently available in PowerShell on Linux/macOS.</span></span> <span data-ttu-id="d8aad-170">Ta funkcja aktualnie nie jest w zakresie 6.0 i coś firma Microsoft będzie post 6.0 wziąć pod uwagę wymaga znaczących projektowe.</span><span class="sxs-lookup"><span data-stu-id="d8aad-170">This feature is currently not in scope for 6.0 and something we will consider post 6.0 as it requires significant design work.</span></span>

### <a name="sudo-exec-and-powershell"></a><span data-ttu-id="d8aad-171">`sudo`, `exec`, jak i programu PowerShell</span><span class="sxs-lookup"><span data-stu-id="d8aad-171">`sudo`, `exec`, and PowerShell</span></span>

<span data-ttu-id="d8aad-172">Ponieważ Środowisko PowerShell jest uruchamiana większość używanych poleceń w pamięci (na przykład Python lub Ruby), nie można użyć sudo bezpośrednio z built-ins środowiska PowerShell. (Mogą oczywiście uruchamiać `powershell` z sudo.) Jeśli jest konieczne uruchomienie polecenia cmdlet programu PowerShell z wewnątrz programu PowerShell z sudo, na przykład `sudo Set-Date 8/18/2016`, a następnie należy `sudo powershell Set-Date 8/18/2016`.</span><span class="sxs-lookup"><span data-stu-id="d8aad-172">Because PowerShell runs most commands in memory (like Python or Ruby), you can't use sudo directly with PowerShell built-ins. (You can, of course, run `powershell` from sudo.) If it is necessary to run a PowerShell cmdlet from within PowerShell with sudo, for example, `sudo Set-Date 8/18/2016`, then you would do `sudo powershell Set-Date 8/18/2016`.</span></span> <span data-ttu-id="d8aad-173">Podobnie nie możesz exec programu PowerShell wbudowanych bezpośrednio.</span><span class="sxs-lookup"><span data-stu-id="d8aad-173">Likewise, you can't exec a PowerShell built-in directly.</span></span> <span data-ttu-id="d8aad-174">Zamiast tego należy wykonać `exec powershell item_to_exec`.</span><span class="sxs-lookup"><span data-stu-id="d8aad-174">Instead you would have to do `exec powershell item_to_exec`.</span></span>

<span data-ttu-id="d8aad-175">Ten problem jest obecnie śledzony jako część #3232.</span><span class="sxs-lookup"><span data-stu-id="d8aad-175">This issue is currently being tracked as part of #3232.</span></span>

### <a name="missing-cmdlets"></a><span data-ttu-id="d8aad-176">Brak polecenia cmdlet</span><span class="sxs-lookup"><span data-stu-id="d8aad-176">Missing Cmdlets</span></span>

<span data-ttu-id="d8aad-177">Dużej liczby poleceń (polecenia cmdlet) zwykle dostępne w programie PowerShell nie są dostępne w systemie Linux/macOS.</span><span class="sxs-lookup"><span data-stu-id="d8aad-177">A large number of the commands (cmdlets) normally available in PowerShell are not available on Linux/macOS.</span></span> <span data-ttu-id="d8aad-178">W wielu przypadkach tych poleceń nie ma sensu na tych platformach (np. funkcje właściwe dla systemu Windows, takie jak rejestru).</span><span class="sxs-lookup"><span data-stu-id="d8aad-178">In many cases, these commands make no sense on these platforms (e.g. Windows-specific features like the registry).</span></span> <span data-ttu-id="d8aad-179">Inne polecenia, takich jak polecenia sterowania usługi (Get/Start/Stop-Service) są dostępne, ale nie działa.</span><span class="sxs-lookup"><span data-stu-id="d8aad-179">Other commands like the service control commands (Get/Start/Stop-Service) are present, but not functional.</span></span> <span data-ttu-id="d8aad-180">Przyszłych wydaniach będzie rozwiązać te problemy, ustalania przerwane poleceń cmdlet i dodaje nowe wraz z upływem czasu.</span><span class="sxs-lookup"><span data-stu-id="d8aad-180">Future releases will correct these problems, fixing the broken cmdlets and adding new ones over time.</span></span>

### <a name="command-availability"></a><span data-ttu-id="d8aad-181">Polecenie dostępności</span><span class="sxs-lookup"><span data-stu-id="d8aad-181">Command Availability</span></span>

<span data-ttu-id="d8aad-182">Poniższa tabela zawiera listę poleceń, które są znane nie będą działać w programie PowerShell w systemie Linux/macOS.</span><span class="sxs-lookup"><span data-stu-id="d8aad-182">The following table lists commands that are known not to work in PowerShell on Linux/macOS.</span></span>

<table>
<th><span data-ttu-id="d8aad-183">Polecenia</span><span class="sxs-lookup"><span data-stu-id="d8aad-183">Commands</span></span></th><th><span data-ttu-id="d8aad-184">Stan operacyjny</span><span class="sxs-lookup"><span data-stu-id="d8aad-184">Operational State</span></span></th><th><span data-ttu-id="d8aad-185">Uwagi</span><span class="sxs-lookup"><span data-stu-id="d8aad-185">Notes</span></span></th>
<tr>
<td><span data-ttu-id="d8aad-186">Get-Service, nowej usługi, Restart-Service, Resume-Service, usługi zestawu, Start-Service, Stop-Service, wstrzymanie usługi</span><span class="sxs-lookup"><span data-stu-id="d8aad-186">Get-Service, New-Service, Restart-Service, Resume-Service, Set-Service, Start-Service, Stop-Service, Suspend-Service</span></span>
<td><span data-ttu-id="d8aad-187">Niedostępne.</span><span class="sxs-lookup"><span data-stu-id="d8aad-187">Not available.</span></span>
<td><span data-ttu-id="d8aad-188">Polecenia te nie zostaną rozpoznane.</span><span class="sxs-lookup"><span data-stu-id="d8aad-188">These commands will not be recognized.</span></span> <span data-ttu-id="d8aad-189">Powinien być rozwiązany w przyszłej wersji.</span><span class="sxs-lookup"><span data-stu-id="d8aad-189">This should be fixed in a future release.</span></span>
</tr>
<tr>
<td><span data-ttu-id="d8aad-190">Listy Acl GET, Set-list kontroli dostępu</span><span class="sxs-lookup"><span data-stu-id="d8aad-190">Get-Acl, Set-Acl</span></span>
<td><span data-ttu-id="d8aad-191">Niedostępne.</span><span class="sxs-lookup"><span data-stu-id="d8aad-191">Not available.</span></span>
<td><span data-ttu-id="d8aad-192">Polecenia te nie zostaną rozpoznane.</span><span class="sxs-lookup"><span data-stu-id="d8aad-192">These commands will not be recognized.</span></span> <span data-ttu-id="d8aad-193">Powinien być rozwiązany w przyszłej wersji.</span><span class="sxs-lookup"><span data-stu-id="d8aad-193">This should be fixed in a future release.</span></span>
</tr>
<tr>
<td><span data-ttu-id="d8aad-194">Get-AuthenticodeSignature, Set-AuthenticodeSignature</span><span class="sxs-lookup"><span data-stu-id="d8aad-194">Get-AuthenticodeSignature, Set-AuthenticodeSignature</span></span>
<td><span data-ttu-id="d8aad-195">Niedostępne.</span><span class="sxs-lookup"><span data-stu-id="d8aad-195">Not available.</span></span>
<td><span data-ttu-id="d8aad-196">Polecenia te nie zostaną rozpoznane.</span><span class="sxs-lookup"><span data-stu-id="d8aad-196">These commands will not be recognized.</span></span> <span data-ttu-id="d8aad-197">Powinien być rozwiązany w przyszłej wersji.</span><span class="sxs-lookup"><span data-stu-id="d8aad-197">This should be fixed in a future release.</span></span>
</tr>
<tr>
<td><span data-ttu-id="d8aad-198">Proces oczekiwania</span><span class="sxs-lookup"><span data-stu-id="d8aad-198">Wait-Process</span></span>
<td><span data-ttu-id="d8aad-199">Dostępne, nie działa prawidłowo.</span><span class="sxs-lookup"><span data-stu-id="d8aad-199">Available, doesn't work properly.</span></span> <td><span data-ttu-id="d8aad-200">Na przykład `Start-Process gvim -PassThru | Wait-Process` nie działa; nie jest on poczekaj, aż proces.</span><span class="sxs-lookup"><span data-stu-id="d8aad-200">For example `Start-Process gvim -PassThru | Wait-Process` doesn't work; it fails to wait for the process.</span></span>
</tr>
<tr>
<td><span data-ttu-id="d8aad-201">Register-PSSessionConfiguration, Unregister-PSSessionConfiguration, Get-PSSessionConfiguration</span><span class="sxs-lookup"><span data-stu-id="d8aad-201">Register-PSSessionConfiguration, Unregister-PSSessionConfiguration, Get-PSSessionConfiguration</span></span>
<td><span data-ttu-id="d8aad-202">Dostępne, ale nie działa.</span><span class="sxs-lookup"><span data-stu-id="d8aad-202">Available but doesn't work.</span></span>
<td><span data-ttu-id="d8aad-203">Zapisuje komunikat o błędzie wskazujący, że nie działają polecenia.</span><span class="sxs-lookup"><span data-stu-id="d8aad-203">Writes an error message indicating that the commands are not working.</span></span> <span data-ttu-id="d8aad-204">Te powinny zostać ustalone w przyszłej wersji.</span><span class="sxs-lookup"><span data-stu-id="d8aad-204">These should be fixed in a future release.</span></span>
</tr>
<tr>
<td><span data-ttu-id="d8aad-205">Get zdarzeń, nowe zdarzenie, EngineEvent rejestru, Register-WmiEvent, zdarzenie usuwania wyrejestrować — zdarzenie</span><span class="sxs-lookup"><span data-stu-id="d8aad-205">Get-Event, New-Event, Register-EngineEvent, Register-WmiEvent, Remove-Event, Unregister-Event</span></span>
<td><span data-ttu-id="d8aad-206">Dostępne są dostępne, ale nie źródła zdarzeń.</span><span class="sxs-lookup"><span data-stu-id="d8aad-206">Available but no event sources are available.</span></span>
<td><span data-ttu-id="d8aad-207">Polecenia programu PowerShell zdarzeń są obecne, ale większość źródła zdarzeń używanych z poleceniami (na przykład System.Timers.Timer) nie są dostępne w systemie Linux wprowadzania polecenia bezużyteczne w wersji alfa.</span><span class="sxs-lookup"><span data-stu-id="d8aad-207">The PowerShell eventing commands are present but most of the event sources used with the commands (such as System.Timers.Timer) are not available on Linux making the commands useless in the Alpha release.</span></span>
</tr>
<tr>
<td><span data-ttu-id="d8aad-208">Set-ExecutionPolicy</span><span class="sxs-lookup"><span data-stu-id="d8aad-208">Set-ExecutionPolicy</span></span>
<td><span data-ttu-id="d8aad-209">Dostępne, ale nie działa.</span><span class="sxs-lookup"><span data-stu-id="d8aad-209">Available but doesn't work.</span></span>
<td><span data-ttu-id="d8aad-210">Zwraca komunikat nie jest obsługiwana na tej platformie.</span><span class="sxs-lookup"><span data-stu-id="d8aad-210">Returns a message saying not supported on this platform.</span></span> <span data-ttu-id="d8aad-211">Zasady wykonywania jest zorientowanych na użytkownika "bezpieczeństwa taśmy" która pomaga zapobiegać wprowadzania kosztowne błędów przez użytkownika.</span><span class="sxs-lookup"><span data-stu-id="d8aad-211">Execution policy is a user-focused "safety belt" that helps prevent the user from making expensive mistakes.</span></span> <span data-ttu-id="d8aad-212">Nie jest granicy zabezpieczeń.</span><span class="sxs-lookup"><span data-stu-id="d8aad-212">It is not a security boundary.</span></span>
</tr>
<tr>
<td><span data-ttu-id="d8aad-213">Nowe PSSessionOption nowe PSTransportOption</span><span class="sxs-lookup"><span data-stu-id="d8aad-213">New-PSSessionOption, New-PSTransportOption</span></span>
<td><span data-ttu-id="d8aad-214">Dostępne, ale New-PSSession nie działa.</span><span class="sxs-lookup"><span data-stu-id="d8aad-214">Available but New-PSSession doesn't work.</span></span>
<td><span data-ttu-id="d8aad-215">New-PSSessionOption i nowy PSTransportOption nie są obecnie weryfikowane pracę teraz, New-PSSession działa.</span><span class="sxs-lookup"><span data-stu-id="d8aad-215">New-PSSessionOption and New-PSTransportOption are not currently verified to work now that New-PSSession works.</span></span>
</tr>
</table>
