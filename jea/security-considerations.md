---
ms.date: 06/12/2017
keywords: jea, programu powershell, zabezpieczeń
title: Zagadnienia dotyczące zabezpieczeń usługi JEA
ms.openlocfilehash: ede727f0f30412d520712d6ba855ba2008375d9a
ms.sourcegitcommit: b6871f21bd666f9cd71dd336bb3f844cf472b56c
ms.translationtype: MT
ms.contentlocale: pl-PL
ms.lasthandoff: 02/03/2019
ms.locfileid: "55687244"
---
# <a name="jea-security-considerations"></a><span data-ttu-id="d27da-103">Zagadnienia dotyczące zabezpieczeń usługi JEA</span><span class="sxs-lookup"><span data-stu-id="d27da-103">JEA Security Considerations</span></span>

> <span data-ttu-id="d27da-104">Dotyczy: Windows PowerShell 5.0</span><span class="sxs-lookup"><span data-stu-id="d27da-104">Applies to: Windows PowerShell 5.0</span></span>

<span data-ttu-id="d27da-105">JEA pomaga zwiększyć poziom bezpieczeństwa dzięki zmniejszeniu liczby administratorów trwałych na maszynach.</span><span class="sxs-lookup"><span data-stu-id="d27da-105">JEA helps you improve your security posture by reducing the number of permanent administrators on your machines.</span></span>
<span data-ttu-id="d27da-106">Robi to przez utworzenie nowego punktu wejścia dla użytkowników do zarządzania systemem (Konfiguracja sesji programu PowerShell), który jest ściśle zablokowana domyślnie, aby uniemożliwić nadużycia.</span><span class="sxs-lookup"><span data-stu-id="d27da-106">It does so by creating a new entry point for users to manage the system (a PowerShell session configuration) which is tightly locked down by default to prevent misuse.</span></span>
<span data-ttu-id="d27da-107">Użytkownicy, którzy muszą niektóre, ale nie nieograniczony dostęp do komputera, aby wykonywać zadania administracyjne można udzielić dostępu do punktu końcowego JEA.</span><span class="sxs-lookup"><span data-stu-id="d27da-107">Users who need some, but not unlimited, access to the machine to perform administrative tasks can be granted access to the JEA endpoint.</span></span>
<span data-ttu-id="d27da-108">Ponieważ JEA umożliwia ich wykonywania polecenia administratora bezpośrednio, bez dostępu administratora, możesz usunąć tych użytkowników z grup zabezpieczeń o wysokim poziomie uprawnień (były użytkownicy standardowi).</span><span class="sxs-lookup"><span data-stu-id="d27da-108">Because JEA allows them to run admin commands without directly having admin access, you can then remove those users from highly privileged security groups (make them standard users).</span></span>

<span data-ttu-id="d27da-109">W tym temacie opisano model zabezpieczeń usługi JEA i najlepsze rozwiązania, które bardziej szczegółowo.</span><span class="sxs-lookup"><span data-stu-id="d27da-109">This topic describes the JEA security model and best practices in more detail.</span></span>

## <a name="run-as-account"></a><span data-ttu-id="d27da-110">Konto Uruchom jako</span><span class="sxs-lookup"><span data-stu-id="d27da-110">Run As account</span></span>

<span data-ttu-id="d27da-111">Każdy punkt końcowy usługi JEA ma konto wyznaczonym "Uruchom jako", czyli konta, w którym użytkownik nawiązujący połączenie czynności.</span><span class="sxs-lookup"><span data-stu-id="d27da-111">Each JEA endpoint has a designated "run as" account, which is the account under which the connecting user's actions are performed.</span></span>
<span data-ttu-id="d27da-112">To konto jest konfigurowane w [plik konfiguracji sesji](session-configurations.md), i konta, możesz wybrać ma istotny wpływ na zabezpieczenia punktu końcowego usługi.</span><span class="sxs-lookup"><span data-stu-id="d27da-112">This account is configurable in the [session configuration file](session-configurations.md), and the account you choose has a significant bearing on the security of your endpoint.</span></span>

<span data-ttu-id="d27da-113">**Kont wirtualnych** to zalecany sposób konfigurowania opcji Uruchom jako konta.</span><span class="sxs-lookup"><span data-stu-id="d27da-113">**Virtual accounts** are the recommended way of configuring the run as account.</span></span>
<span data-ttu-id="d27da-114">Kont wirtualnych są jednorazowe, tymczasowy kont lokalnych, które są tworzone dla użytkownik nawiązujący połączenie do użycia w czasie trwania sesji JEA.</span><span class="sxs-lookup"><span data-stu-id="d27da-114">Virtual accounts are one-time, temporary local accounts that are created for the connecting user to use during the duration of their JEA session.</span></span>
<span data-ttu-id="d27da-115">Zaraz po ich sesja została przerwana, wirtualnego konta zostaną usunięte i nie można już używać.</span><span class="sxs-lookup"><span data-stu-id="d27da-115">As soon as their session is terminated, the virtual account will be destroyed and cannot be used anymore.</span></span>
<span data-ttu-id="d27da-116">Użytkownik nawiązujący połączenie nie może określić poświadczenia dla konta wirtualnego i nie można używać konta wirtualnej dostęp do systemu za pomocą innych środków, takich jak pulpitu zdalnego lub nieograniczonego punktu końcowego programu PowerShell.</span><span class="sxs-lookup"><span data-stu-id="d27da-116">The connecting user does not know the credentials for the virtual account and cannot use the virtual account to access the system via other means, such as Remote Desktop or an unconstrained PowerShell endpoint.</span></span>

<span data-ttu-id="d27da-117">Domyślnie kont wirtualnych należeć do lokalnej grupy administratorów na komputerze.</span><span class="sxs-lookup"><span data-stu-id="d27da-117">By default, virtual accounts belong to the local administrators group on the machine.</span></span>
<span data-ttu-id="d27da-118">To daje im pełne prawa do niczego w systemie zarządzania, ale żadnych praw do zarządzania zasobami w sieci.</span><span class="sxs-lookup"><span data-stu-id="d27da-118">This gives them full rights to manage anything on the system, but no rights to manage resources on the network.</span></span>
<span data-ttu-id="d27da-119">Podczas uwierzytelniania z innymi maszynami, kontekst użytkownika będzie z konta komputera lokalnego konta wirtualnego.</span><span class="sxs-lookup"><span data-stu-id="d27da-119">When authenticating with other machines, the user context will be that of the local computer account, not the virtual account.</span></span>

<span data-ttu-id="d27da-120">Kontrolery domeny są szczególny przypadek, ponieważ nie obowiązuje koncepcja lokalnej grupy administratorów.</span><span class="sxs-lookup"><span data-stu-id="d27da-120">Domain controllers are a special case since there is no concept of a local administrators group.</span></span>
<span data-ttu-id="d27da-121">Zamiast kont wirtualnych należą administratorzy domeny i zarządzać usługi katalogowe na kontrolerze domeny.</span><span class="sxs-lookup"><span data-stu-id="d27da-121">Instead, virtual accounts belong to Domain Admins instead and can manage the directory services on the domain controller.</span></span>
<span data-ttu-id="d27da-122">Tożsamość domeny jest nadal ograniczona do użycia na kontrolerze domeny, której wystąpienia sesji JEA i dostępu do żadnych sieci pojawią się pochodzić z obiektu komputera kontrolera domeny, zamiast tego.</span><span class="sxs-lookup"><span data-stu-id="d27da-122">The domain identity is still restricted to use on the domain controller where the JEA session was instantiated, and any network access will appear to come from the domain controller computer object instead.</span></span>

<span data-ttu-id="d27da-123">W obu przypadkach można także jawnie zdefiniować grupy zabezpieczeń, które konta wirtualnej powinny należeć do.</span><span class="sxs-lookup"><span data-stu-id="d27da-123">In both cases, you can also explicitly define which security groups the virtual account should belong to.</span></span>
<span data-ttu-id="d27da-124">Jest dobrą praktyką, gdy zadanie, które wykonujesz, możesz zrobić bez uprawnień administratora lokalnego/domeny.</span><span class="sxs-lookup"><span data-stu-id="d27da-124">This is a good practice when the task you are performing can be done without local/domain admin privileges.</span></span>
<span data-ttu-id="d27da-125">Jeśli masz już grupę zabezpieczeń zdefiniowane dla administratorów, można po prostu udzielić członkostwa wirtualnego konta do tej grupy w celu nadania mu uprawnień, których potrzebuje.</span><span class="sxs-lookup"><span data-stu-id="d27da-125">If you already have a security group defined for your admins, you can simply grant the virtual account membership to that group to give it the permissions it needs.</span></span>
<span data-ttu-id="d27da-126">Wirtualny, członkostwo w grupie kont jest ograniczona do lokalnych grup zabezpieczeń na stacji roboczej i elementów członkowskich serwerach, ale na kontrolerze domeny mogą być tylko członkowie grupy zabezpieczeń domeny.</span><span class="sxs-lookup"><span data-stu-id="d27da-126">Virtual account group membership is limited to local security groups on workstation and member servers, but on a domain controller they can only be members of domain security groups.</span></span>
<span data-ttu-id="d27da-127">Po określeniu jednego lub więcej grup zabezpieczeń dla konta wirtualnej należy do, już nie będą należeć do grup domyślnych (administratora lokalnego lub administratora domeny).</span><span class="sxs-lookup"><span data-stu-id="d27da-127">Once you specify one or more security groups for the virtual account to belong to, it will no longer belong to the default groups (local admin or domain admin).</span></span>

<span data-ttu-id="d27da-128">W poniższej tabeli podsumowano możliwych opcji konfiguracji i wynikowy uprawnienia dla kont wirtualnych</span><span class="sxs-lookup"><span data-stu-id="d27da-128">The table below summarizes the possible configuration options and resulting permissions for virtual accounts</span></span>

<span data-ttu-id="d27da-129">Typ komputera</span><span class="sxs-lookup"><span data-stu-id="d27da-129">Computer type</span></span>                | <span data-ttu-id="d27da-130">Konfiguracja grupy konta wirtualnego</span><span class="sxs-lookup"><span data-stu-id="d27da-130">Virtual account group configuration</span></span> | <span data-ttu-id="d27da-131">Kontekst użytkownika lokalnego</span><span class="sxs-lookup"><span data-stu-id="d27da-131">Local user context</span></span>                                      | <span data-ttu-id="d27da-132">Kontekst użytkownika sieci</span><span class="sxs-lookup"><span data-stu-id="d27da-132">Network user context</span></span>
-----------------------------|-------------------------------------|---------------------------------------------------------|--------------------------------------------------
<span data-ttu-id="d27da-133">Kontroler domeny</span><span class="sxs-lookup"><span data-stu-id="d27da-133">Domain controller</span></span>            | <span data-ttu-id="d27da-134">Wartość domyślna</span><span class="sxs-lookup"><span data-stu-id="d27da-134">Default</span></span>                             | <span data-ttu-id="d27da-135">Użytkownik domeny, członek "*domeny*\Domain</span><span class="sxs-lookup"><span data-stu-id="d27da-135">Domain user, member of '*DOMAIN*\Domain Admins'</span></span>         | <span data-ttu-id="d27da-136">Konto komputera</span><span class="sxs-lookup"><span data-stu-id="d27da-136">Computer account</span></span>
<span data-ttu-id="d27da-137">Kontroler domeny</span><span class="sxs-lookup"><span data-stu-id="d27da-137">Domain controller</span></span>            | <span data-ttu-id="d27da-138">Grupy w domenie A i B</span><span class="sxs-lookup"><span data-stu-id="d27da-138">Domain groups A and B</span></span>               | <span data-ttu-id="d27da-139">Użytkownik domeny, członek "*domeny*\A","*domeny*\B"</span><span class="sxs-lookup"><span data-stu-id="d27da-139">Domain user, member of '*DOMAIN*\A', '*DOMAIN*\B'</span></span>       | <span data-ttu-id="d27da-140">Konto komputera</span><span class="sxs-lookup"><span data-stu-id="d27da-140">Computer account</span></span>
<span data-ttu-id="d27da-141">Serwera członkowskiego lub stacji roboczej</span><span class="sxs-lookup"><span data-stu-id="d27da-141">Member server or workstation</span></span> | <span data-ttu-id="d27da-142">Wartość domyślna</span><span class="sxs-lookup"><span data-stu-id="d27da-142">Default</span></span>                             | <span data-ttu-id="d27da-143">Użytkownik lokalny, członek "*BUILTIN*\Administrators</span><span class="sxs-lookup"><span data-stu-id="d27da-143">Local user, member of '*BUILTIN*\Administrators'</span></span>        | <span data-ttu-id="d27da-144">Konto komputera</span><span class="sxs-lookup"><span data-stu-id="d27da-144">Computer account</span></span>
<span data-ttu-id="d27da-145">Serwera członkowskiego lub stacji roboczej</span><span class="sxs-lookup"><span data-stu-id="d27da-145">Member server or workstation</span></span> | <span data-ttu-id="d27da-146">Grupy lokalne, C i D</span><span class="sxs-lookup"><span data-stu-id="d27da-146">Local groups C and D</span></span>                | <span data-ttu-id="d27da-147">Użytkownik lokalny, członek "*komputera*\C" i "*komputera*\D"</span><span class="sxs-lookup"><span data-stu-id="d27da-147">Local user, member of '*COMPUTER*\C' and '*COMPUTER*\D'</span></span> | <span data-ttu-id="d27da-148">Konto komputera</span><span class="sxs-lookup"><span data-stu-id="d27da-148">Computer account</span></span>

<span data-ttu-id="d27da-149">Jeśli przyjrzymy się zdarzenia inspekcji zabezpieczeń i dzienniki zdarzeń aplikacji, zobaczysz, czy każdej sesji użytkownika JEA ma unikatowe konto wirtualne.</span><span class="sxs-lookup"><span data-stu-id="d27da-149">When you look at security audit events and application event logs, you will see that each JEA user session has a unique virtual account.</span></span>
<span data-ttu-id="d27da-150">Dzięki temu można śledzić akcje użytkownika w punkcie końcowym usługi JEA dla oryginalnego użytkownika, który uruchomił polecenie.</span><span class="sxs-lookup"><span data-stu-id="d27da-150">This helps you track user actions in a JEA endpoint back to the original user who ran the command.</span></span>
<span data-ttu-id="d27da-151">Konta wirtualnej nazwy zgodne z formatem "Użytkownicy usługi WinRM wirtualnej\\WinRM\_oceny luk w zabezpieczeniach\_*ACCOUNTNUMBER*\_*domeny* \_ *sAMAccountName*"na przykład, jeśli użytkownik"Alicja"w domenie"Contoso"ponowne uruchomienie usługi w punktu końcowego JEA, nazwę użytkownika skojarzoną z dowolnego zdarzenia Menedżera sterowania usługami będzie" użytkowników wirtualnych WinRM\\WinRM\_ Oceny luk w zabezpieczeniach\_1\_contoso\_Alicja ".</span><span class="sxs-lookup"><span data-stu-id="d27da-151">Virtual account names follow the format "WinRM Virtual Users\\WinRM\_VA\_*ACCOUNTNUMBER*\_*DOMAIN*\_*sAMAccountName*" For example, if user "Alice" in domain "Contoso" restarts a service in a JEA endpoint, the username associated with any service control manager events would be "WinRM Virtual Users\\WinRM\_VA\_1\_contoso\_alice".</span></span>


<span data-ttu-id="d27da-152">**Grupa kont usług zarządzanych (kont Gmsa)** są przydatne, gdy serwer członkowski musi mieć dostęp do zasobów sieciowych w sesji JEA.</span><span class="sxs-lookup"><span data-stu-id="d27da-152">**Group managed service accounts (gMSAs)** are useful when a member server needs to have access to network resources in the JEA session.</span></span>
<span data-ttu-id="d27da-153">Przykład przypadek użycia to jest JEA punktu końcowego, który służy do kontrolowania dostępu do interfejsu API REST hostowany na innym komputerze.</span><span class="sxs-lookup"><span data-stu-id="d27da-153">An example use case for this is a JEA endpoint that is used to control access to a REST API hosted on a different machine.</span></span>
<span data-ttu-id="d27da-154">To ułatwia pisanie funkcji dzięki żądaną wywołań interfejsu API REST, ale w celu uwierzytelnienia przy użyciu interfejsu API potrzebne tożsamości sieciowej.</span><span class="sxs-lookup"><span data-stu-id="d27da-154">It is easy to write functions to make the desired invocations on the REST API, but in order to authenticate with the API you need a network identity.</span></span>
<span data-ttu-id="d27da-155">Za pomocą konta usługi zarządzanych przez grupę pozwala "drugiego przeskoku" przy zachowaniu kontroli nad tym, którzy komputery można używać tego konta.</span><span class="sxs-lookup"><span data-stu-id="d27da-155">Using a group managed service account makes the "second hop" possible while still having control over which computers can use the account.</span></span>
<span data-ttu-id="d27da-156">Czynne uprawnienia opcją gMSA są definiowane przez grupy zabezpieczeń (lokalnego lub domeny) do którego należy dane konta gMSA.</span><span class="sxs-lookup"><span data-stu-id="d27da-156">The effective permissions of the gMSA are defined by the security groups (local or domain) to which the gMSA account belongs.</span></span>

<span data-ttu-id="d27da-157">Gdy punkt końcowy usługi JEA jest skonfigurowany do używania konta gMSA, akcje dla wszystkich użytkowników usługi JEA pojawi się pochodzić z tej samej grupy zarządzane konto usługi.</span><span class="sxs-lookup"><span data-stu-id="d27da-157">When a JEA endpoint is configured to use a gMSA account, the actions of all JEA users will appear to come from the same group managed service account.</span></span>
<span data-ttu-id="d27da-158">Jest to jedyny sposób, można śledzić akcje do określonego użytkownika do identyfikowania zestawów polecenia uruchamiane w wykazie sesji programu PowerShell.</span><span class="sxs-lookup"><span data-stu-id="d27da-158">The only way you can trace actions back to a specific user is to identify the set of commands run in a PowerShell session transcript.</span></span>

<span data-ttu-id="d27da-159">**Przekaż do poświadczeń** są używane, gdy nie określaj Uruchom jako konto się i nie ma programu PowerShell, Uruchom polecenia na serwerze zdalnym za pomocą poświadczeń użytkownik nawiązujący połączenie.</span><span class="sxs-lookup"><span data-stu-id="d27da-159">**Pass-thru credentials** are used when you do not specify a run as account and want PowerShell to use the connecting user's credential to run commands on the remote server.</span></span>
<span data-ttu-id="d27da-160">Ta konfiguracja jest *nie* zalecane dla usługi JEA, ponieważ wymagałoby przyznania łączące bezpośredni dostęp użytkowników do grup uprzywilejowanych zarządzania.</span><span class="sxs-lookup"><span data-stu-id="d27da-160">This configuration is *not* recommended for JEA as it would require you to grant the connecting user direct access to privileged management groups.</span></span>
<span data-ttu-id="d27da-161">Jeśli użytkownik nawiązujący połączenie ma już uprawnienia administratora, są całkowicie uniknąć JEA i zarządzania systemem za pośrednictwem oznacza, że inne, nieograniczone.</span><span class="sxs-lookup"><span data-stu-id="d27da-161">If the connecting user already has admin privileges, they can avoid JEA altogether and manage the system via other, unconstrained means.</span></span>
<span data-ttu-id="d27da-162">Zobacz w poniższym rozdziale na temat [JEA nie chroni przed Administratorzy](#jea-does-not-protect-against-admins) Aby uzyskać więcej informacji.</span><span class="sxs-lookup"><span data-stu-id="d27da-162">See the section below on how [JEA does not protect against admins](#jea-does-not-protect-against-admins) for more information.</span></span>

<span data-ttu-id="d27da-163">**Standardowa Uruchom jako konta** pozwalają na określenie dowolnego konta użytkownika, na którym będzie uruchamiana podczas całej sesji programu PowerShell.</span><span class="sxs-lookup"><span data-stu-id="d27da-163">**Standard run as accounts** allow you to specify any user account under which the entire PowerShell session will run.</span></span>
<span data-ttu-id="d27da-164">Jest to ważna różnica, ponieważ konfiguracja sesji skonfigurowany do używania stały Uruchom jako konto (przy użyciu `-RunAsCredential` parametr) nie są znane JEA.</span><span class="sxs-lookup"><span data-stu-id="d27da-164">This is an important distinction, because a session configuration set to use a fixed run as account (with the `-RunAsCredential` parameter) is not JEA-aware.</span></span>
<span data-ttu-id="d27da-165">Oznacza to, że definicje ról przestaną działać zgodnie z oczekiwaniami, a każdy użytkownik autoryzowany do dostępu do punktu końcowego zostanie przypisany tę samą rolę.</span><span class="sxs-lookup"><span data-stu-id="d27da-165">That means that role definitions no longer function as expected, and every user authorized to access the endpoint will be assigned the same role.</span></span>

<span data-ttu-id="d27da-166">Nie należy używać RunAsCredential w punkcie końcowym usługi JEA ze względu na problemy w śledzeniu akcje do konkretnych użytkowników i brak obsługi mapowanie użytkowników do ról.</span><span class="sxs-lookup"><span data-stu-id="d27da-166">You should not use a RunAsCredential on a JEA endpoint because of the difficulty in tracing actions back to specific users and the lack of support for mapping users to roles.</span></span>

## <a name="winrm-endpoint-acl"></a><span data-ttu-id="d27da-167">Listy ACL punktów końcowych usługi WinRM</span><span class="sxs-lookup"><span data-stu-id="d27da-167">WinRM Endpoint ACL</span></span>

<span data-ttu-id="d27da-168">Za pomocą regularnego punkty końcowe komunikacji zdalnej programu PowerShell, każdego punktu końcowego JEA ma oddzielne listy kontroli dostępu (ACL) w konfiguracji usługi WinRM, który kontroluje kto może uwierzytelniać przy użyciu punktu końcowego JEA.</span><span class="sxs-lookup"><span data-stu-id="d27da-168">As with regular PowerShell remoting endpoints, each JEA endpoint has a separate access control list (ACL) set in the WinRM configuration that controls who can authenticate with the JEA endpoint.</span></span>
<span data-ttu-id="d27da-169">Jeśli skonfigurowana niepoprawnie, zaufanych użytkownicy mogą nie mieć możliwość dostępu do punktu końcowego JEA i/lub niezaufanym użytkownikom może uzyskać dostęp.</span><span class="sxs-lookup"><span data-stu-id="d27da-169">If improperly configured, trusted users may not be able to access the JEA endpoint and/or untrusted users may gain access.</span></span>
<span data-ttu-id="d27da-170">Listy ACL usługi WinRM nie dotyczy jednak mapowanie użytkowników do ról usługi JEA.</span><span class="sxs-lookup"><span data-stu-id="d27da-170">The WinRM ACL does not, however, affect the mapping of users to JEA roles.</span></span>
<span data-ttu-id="d27da-171">Który jest kontrolowany przez *RoleDefinitions* pola w pliku konfiguracji sesji, który został zarejestrowany w systemie.</span><span class="sxs-lookup"><span data-stu-id="d27da-171">That is controlled by the *RoleDefinitions* field in the session configuration file that was registered on the system.</span></span>

<span data-ttu-id="d27da-172">Domyślnie podczas rejestracji, punktu końcowego JEA przy użyciu pliku konfiguracji sesji i jedną lub kilkoma funkcjami roli listy ACL usługi WinRM zostaną skonfigurowane aby umożliwić wszystkim użytkownikom mapowanie na jeden lub więcej ról uprawnieniami do punktu końcowego.</span><span class="sxs-lookup"><span data-stu-id="d27da-172">By default, when you register a JEA endpoint using a session configuration file and one or more role capabilities, the WinRM ACL will be configured to allow all users mapping to one or more roles access to the endpoint.</span></span>
<span data-ttu-id="d27da-173">Na przykład sesję JEA skonfigurowany przy użyciu następujących poleceń spowoduje przyznanie pełnego dostępu do *CONTOSO\JEA\_Lev1* i *CONTOSO\JEA\_Lev2*.</span><span class="sxs-lookup"><span data-stu-id="d27da-173">For example, a JEA session configured using the following commands will grant full access to *CONTOSO\JEA\_Lev1* and *CONTOSO\JEA\_Lev2*.</span></span>

```powershell
$roles = @{ 'CONTOSO\JEA_Lev1' = 'Lev1Role'; 'CONTOSO\JEA_Lev2' = 'Lev2Role' }
New-PSSessionConfigurationFile -Path '.\jea.pssc' -SessionType RestrictedRemoteServer -RoleDefinitions $roles -RunAsVirtualAccount
Register-PSSessionConfiguration -Path '.\jea.pssc' -Name 'MyJEAEndpoint'
```

<span data-ttu-id="d27da-174">Można przeprowadzać ich inspekcje uprawnień użytkownika za pomocą [Get-PSSessionConfiguration](https://msdn.microsoft.com/powershell/reference/5.1/microsoft.powershell.core/get-pssessionconfiguration) polecenia cmdlet.</span><span class="sxs-lookup"><span data-stu-id="d27da-174">You can audit user permissions with the [Get-PSSessionConfiguration](https://msdn.microsoft.com/powershell/reference/5.1/microsoft.powershell.core/get-pssessionconfiguration) cmdlet.</span></span>

```powershell
PS C:\> Get-PSSessionConfiguration -Name 'MyJEAEndpoint' | Select-Object Permission

Permission
----------
CONTOSO\JEA_Lev1 AccessAllowed
CONTOSO\JEA_Lev2 AccessAllowed
```

<span data-ttu-id="d27da-175">Aby zmienić, którzy użytkownicy mają dostęp, uruchom `Set-PSSessionConfiguration -Name 'MyJEAEndpoint' -ShowSecurityDescriptorUI` dla monitu interakcyjnego lub `Set-PSSessionConfiguration -Name 'MyJEAEndpoint' -SecurityDescriptorSddl <SDDL string>` można zaktualizować uprawnień.</span><span class="sxs-lookup"><span data-stu-id="d27da-175">To change which users have access, run either `Set-PSSessionConfiguration -Name 'MyJEAEndpoint' -ShowSecurityDescriptorUI` for an interactive prompt or `Set-PSSessionConfiguration -Name 'MyJEAEndpoint' -SecurityDescriptorSddl <SDDL string>` to update the permissions.</span></span>
<span data-ttu-id="d27da-176">Użytkownicy muszą się przynajmniej *Invoke* praw dostępu do punktu końcowego JEA.</span><span class="sxs-lookup"><span data-stu-id="d27da-176">Users need at least *Invoke* rights to access the JEA endpoint.</span></span>

<span data-ttu-id="d27da-177">Jeśli dodatkowi użytkownicy uzyskują dostęp do punktu końcowego JEA, ale nie należą do żadnych ról zdefiniowane w pliku konfiguracji sesji, będą mogli rozpocząć sesję JEA, ale tylko mają dostęp do poleceń cmdlet programu domyślnego.</span><span class="sxs-lookup"><span data-stu-id="d27da-177">If additional users are granted access to the JEA endpoint but do not fall into any of the roles defined in the session configuration file, they will be able to start a JEA session but only have access to the default cmdlets.</span></span>
<span data-ttu-id="d27da-178">Można przeprowadzać ich inspekcje uprawnienia użytkownika w punkcie końcowym usługi JEA, uruchamiając `Get-PSSessionCapability`.</span><span class="sxs-lookup"><span data-stu-id="d27da-178">You can audit user permissions in a JEA endpoint by running `Get-PSSessionCapability`.</span></span>
<span data-ttu-id="d27da-179">Zapoznaj się z [inspekcji i raporty dotyczące technologii JEA](audit-and-report.md) artykuł, aby uzyskać więcej informacji na temat inspekcji polecenia użytkownika, które ma dostęp do w punktu końcowego JEA.</span><span class="sxs-lookup"><span data-stu-id="d27da-179">Check out the [Auditing and Reporting on JEA](audit-and-report.md) article for more information about auditing which commands a user has access to in a JEA endpoint.</span></span>

## <a name="least-privilege-roles"></a><span data-ttu-id="d27da-180">Co najmniej role uprawnień</span><span class="sxs-lookup"><span data-stu-id="d27da-180">Least privilege roles</span></span>

<span data-ttu-id="d27da-181">Podczas projektowania technologii JEA ról, to należy pamiętać, że wirtualny lub grupy usługi zarządzanej konta działa w tle często ma nieograniczony dostęp do funkcji zarządzania na komputerze lokalnym.</span><span class="sxs-lookup"><span data-stu-id="d27da-181">When designing JEA roles, it is important to remember that the virtual or group managed service account running behind the scenes often has unrestricted access to manage the local machine.</span></span>
<span data-ttu-id="d27da-182">Możliwości roli JEA pomagają ograniczyć, jakie to konto może służyć do, ograniczając polecenia i aplikacje, które mogą być uruchamiane przy użyciu tego kontekstu uprzywilejowanych.</span><span class="sxs-lookup"><span data-stu-id="d27da-182">JEA role capabilities help restrict what that account can be used for by limiting the commands and applications that can be run using that privileged context.</span></span>
<span data-ttu-id="d27da-183">Nieprawidłowo zaprojektowane role można zezwolić na niebezpiecznych uruchamiania poleceń, które umożliwiają użytkownikowi zerwać granice JEA lub uzyskać dostępu do poufnych informacji.</span><span class="sxs-lookup"><span data-stu-id="d27da-183">Improperly designed roles can allow dangerous commands to run that may allow a user to break out of the JEA boundaries or obtain access to sensitive information.</span></span>

<span data-ttu-id="d27da-184">Na przykład rozważmy następujący wpis możliwości roli:</span><span class="sxs-lookup"><span data-stu-id="d27da-184">For example, consider the following role capability entry:</span></span>

```powershell
@{
    VisibleCmdlets = 'Microsoft.PowerShell.Management\*-Process'
}
```

<span data-ttu-id="d27da-185">Ta funkcja roli umożliwia użytkownikom uruchamianie każdego polecenia cmdlet programu PowerShell przy użyciu rzeczownik "Procesu" z modułu Microsoft.PowerShell.Management.</span><span class="sxs-lookup"><span data-stu-id="d27da-185">This role capability allows users to run any PowerShell cmdlet with the noun "Process" from the Microsoft.PowerShell.Management module.</span></span>
<span data-ttu-id="d27da-186">Użytkownicy może być konieczne dostęp do poleceń cmdlet, takich jak `Get-Process` Aby zrozumieć, jakie aplikacje działają w systemie i `Stop-Process` kill dowolne zawiesiła się w aplikacji.</span><span class="sxs-lookup"><span data-stu-id="d27da-186">Users may need to access cmdlets like `Get-Process` to understand what applications are running on the system and `Stop-Process` to kill any hung applications.</span></span>
<span data-ttu-id="d27da-187">Jednak ten wpis umożliwia także `Start-Process`, który może służyć do uruchamiania dowolnego programu z uprawnieniami administrator o pełnych uprawnieniach.</span><span class="sxs-lookup"><span data-stu-id="d27da-187">However, this entry also allows `Start-Process`, which can be used to start up an arbitrary program with full administrator permissions.</span></span>
<span data-ttu-id="d27da-188">Program nie musi być zainstalowana lokalnie w systemie, dzięki czemu osoba atakująca może po prostu uruchomić program w udziale plików, zapewniającej połączenia użytkownikowi uprawnienia administratora lokalnego, uruchomienia złośliwego oprogramowania i nie tylko. "</span><span class="sxs-lookup"><span data-stu-id="d27da-188">The program doesn't need to be installed locally on the system, so an adversary could simply start a program on a file share that gives the connecting user local admin privileges, runs malware, and more.'</span></span>

<span data-ttu-id="d27da-189">Bardziej bezpieczna wersja ta sama funkcja roli będzie wyglądać:</span><span class="sxs-lookup"><span data-stu-id="d27da-189">A more secure version of this same role capability would look like:</span></span>

```powershell
@{
    VisibleCmdlets = 'Microsoft.PowerShell.Management\Get-Process', 'Microsoft.PowerShell.Management\Stop-Process'
}
```

<span data-ttu-id="d27da-190">Unikaj używania symboli wieloznacznych w możliwości roli i pamiętaj, aby [inspekcji uprawnienia obowiązująca nazwa użytkownika](audit-and-report.md#check-effective-rights-for-a-specific-user) regularnie, aby zrozumieć, które polecenia użytkownik ma dostęp do.</span><span class="sxs-lookup"><span data-stu-id="d27da-190">Avoid using wildcards in role capabilities, and be sure to [audit effective user permissions](audit-and-report.md#check-effective-rights-for-a-specific-user) regularly to understand which commands a user has access to.</span></span>

## <a name="jea-does-not-protect-against-admins"></a><span data-ttu-id="d27da-191">JEA nie chroni przed administratorów</span><span class="sxs-lookup"><span data-stu-id="d27da-191">JEA does not protect against admins</span></span>

<span data-ttu-id="d27da-192">Jedną z podstawowych zasad JEA jest możliwość użytkowników innych niż administratorzy w celu wykonania *niektóre* zadań administracyjnych.</span><span class="sxs-lookup"><span data-stu-id="d27da-192">One of the core principles of JEA is that it allows non-admins to perform *some* admin tasks.</span></span>
<span data-ttu-id="d27da-193">JEA nie chroni przed tych, którzy już mają uprawnienia administratora.</span><span class="sxs-lookup"><span data-stu-id="d27da-193">JEA does not protect against those who already have administrator privileges.</span></span>
<span data-ttu-id="d27da-194">Użytkownicy którzy należą "Administratorzy domeny," "Administratorzy lokalni" lub innych wysoce uprzywilejowanych grup w danym środowisku nadal będzie można obejść zabezpieczenia JEA firmy, logując się do komputera za pomocą innych metod.</span><span class="sxs-lookup"><span data-stu-id="d27da-194">Users who belong "domain admins," "local admins," or other highly privileged groups in your environment will still be able to get around JEA's protections by signing into the machine via another means.</span></span>
<span data-ttu-id="d27da-195">One można na przykład, zaloguj się przy użyciu protokołu RDP, użyj zdalnych konsol programu MMC lub nawiązać połączenie z użyciem środowiska PowerShell punktów końcowych.</span><span class="sxs-lookup"><span data-stu-id="d27da-195">They could, for example, sign in with RDP, use remote MMC consoles, or connect to unconstrained PowerShell endpoints.</span></span>
<span data-ttu-id="d27da-196">Administratorzy lokalni w systemie można również zmodyfikować konfiguracje JEA, aby umożliwić innym użytkownikom do zarządzania systemem lub zmienić możliwości roli, aby rozszerzyć zakres co użytkownik może zrobić w ich sesji usługi JEA.</span><span class="sxs-lookup"><span data-stu-id="d27da-196">Local admins on the system can also modify JEA configurations to allow additional users to manage the system or change a role capability to extend the scope of what a user can do in their JEA session.</span></span>
<span data-ttu-id="d27da-197">W związku z tym jest ważne, aby ocenić użytkowników usługi JEA rozszerzone uprawnienia, aby zobaczyć, czy istnieją inne sposoby ich może uzyskać uprzywilejowany dostęp do systemu.</span><span class="sxs-lookup"><span data-stu-id="d27da-197">It is therefore important to evaluate your JEA users' extended permissions to see if there are other ways they could gain privileged access to the system.</span></span>

<span data-ttu-id="d27da-198">Powszechną praktyką jest korzystają z technologii JEA dla codziennej konserwacji i mają "dokładnie na czas" uprzywilejowany rozwiązania do zarządzania dostępem Zezwalaj użytkownikom na tymczasowo stają się administratorami lokalnymi sytuacji awaryjnych.</span><span class="sxs-lookup"><span data-stu-id="d27da-198">A common practice is to use JEA for regular day-to-day maintenance and have a "just in time" privileged access management solution allow users to temporarily become local admins in emergency situations.</span></span>
<span data-ttu-id="d27da-199">Dzięki temu użytkownicy nie są trwałe Administratorzy w systemie, ale można uzyskać te prawa, jeśli i tylko wtedy, gdy ich zakończeniu przepływu pracy, która dokumentuje ich użycie tych uprawnień.</span><span class="sxs-lookup"><span data-stu-id="d27da-199">This helps ensure users are not permanent admins on the system, but can get those rights if, and only when, they complete a workflow that documents their use of those permissions.</span></span>